// Your practice space. Run `npm run dev` to rerun this file when you save.
// A complete example lives in examples/oop.ts. Run it with `npm run example`.

console.log("TypeScript OOP playground is ready!");

// Practice: build a small library system, one pillar at a time.
// 1. Encapsulation: create a Book with a private availability field.
//    Expose borrow(), returnBook(), and isAvailable() methods.
//    Prevent borrowing an already borrowed book.
// 2. Abstraction: create an abstract LibraryItem with an abstract describe().
// 3. Inheritance: make Book and Magazine extend LibraryItem.
// 4. Polymorphism: put both in a LibraryItem[] and call describe() on each.

export {};
